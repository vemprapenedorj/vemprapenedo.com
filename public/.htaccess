# Compress HTML, CSS, JS, XML and SVG
<IfModule mod_deflate.c>
  AddOutputFilterByType DEFLATE text/html text/plain text/xml text/css application/javascript application/json image/svg+xml
</IfModule>

# Caching for Static Assets
<IfModule mod_expires.c>
  ExpiresActive On
  ExpiresDefault "access plus 1 month"

  # Images
  ExpiresByType image/jpeg "access plus 30 days"
  ExpiresByType image/png "access plus 30 days"
  ExpiresByType image/gif "access plus 30 days"
  ExpiresByType image/webp "access plus 30 days"
  ExpiresByType image/avif "access plus 30 days"
  ExpiresByType image/svg+xml "access plus 30 days"
  ExpiresByType image/x-icon "access plus 30 days"

  # Fonts
  ExpiresByType font/ttf "access plus 1 year"
  ExpiresByType font/otf "access plus 1 year"
  ExpiresByType font/woff "access plus 1 year"
  ExpiresByType font/woff2 "access plus 1 year"

  # CSS / JS
  ExpiresByType text/css "access plus 1 year"
  ExpiresByType application/javascript "access plus 1 year"
</IfModule>

<IfModule mod_setenvif.c>
  SetEnvIfNoCase Host "^(homologacao\.)?vemprapenedo\.com(?::[0-9]+)?$" IS_TEST_ENV
</IfModule>

<IfModule mod_headers.c>
  # Os ambientes temporários .com nunca devem ser indexados ou arquivados.
  Header always set X-Robots-Tag "noindex, nofollow, noarchive" env=IS_TEST_ENV

  # Set strict cache policies for versioned build assets
  <FilesMatch "\.(css|js|woff2?)$">
    Header set Cache-Control "max-age=31536000, public, immutable"
  </FilesMatch>

  <FilesMatch "\.(png|jpe?g|webp|avif|gif|ico|svg)$">
    Header set Cache-Control "max-age=2592000, public"
  </FilesMatch>

  Header always set X-Content-Type-Options "nosniff"
  Header always set Referrer-Policy "strict-origin-when-cross-origin"
  Header always set Permissions-Policy "geolocation=(), microphone=(), camera=()"

  # Prevent caching of index.html, sitemap.xml and JSON to allow updates
  <FilesMatch "\.(html|xml|json)$">
    Header set Cache-Control "max-age=0, no-cache, no-store, must-revalidate"
  </FilesMatch>
</IfModule>
# Serve um robots.txt restritivo nos ambientes temporários .com.
RewriteEngine On
RewriteCond %{HTTP_HOST} ^(homologacao\.)?vemprapenedo\.com$ [NC]
RewriteRule ^robots\.txt$ /robots-staging.txt [L]

# HTTPS preserva o host; somente hosts públicos são canonicalizados.
RewriteCond %{HTTPS} !=on
RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [R=301,L]

RewriteCond %{HTTP_HOST} ^www\.vemprapenedo\.com$ [NC]
RewriteRule ^ https://vemprapenedo.com%{REQUEST_URI} [R=301,L]

ErrorDocument 404 /404.html
